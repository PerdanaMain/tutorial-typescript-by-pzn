// Tipe data primit dalam typescript --> menggunakan strongly type

const fullName: string = "Firman Perdana";
const balance: number = 10000;
const isVip: boolean = true;

console.log(fullName, balance, isVip);
